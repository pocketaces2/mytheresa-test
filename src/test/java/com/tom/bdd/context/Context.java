package com.tom.bdd.context;


/**
 * Add context enum keys here that can be used to grab specific elements on test data from the
 * scenario context.
 */
public enum Context {
  DEPARTMENT_HYPERLINKS, PAGE_ERRORS;
}
